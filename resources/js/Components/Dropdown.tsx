import { ReactNode } from "react";

// Placeholder component to satisfy potential imports.
// If your project already has this component, remove this file.
export default function Dropdown({ children }: { children: ReactNode }) {
    return <>{children}</>;
}
